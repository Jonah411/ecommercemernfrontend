export const categoriesCollect = (list, parent, datalist) => {
  // console.log(list, parent);
  //   let parentData = [];
  //   for (var i = 0; i < list?.length; i++) {
  //     parentData?.push(
  //       list?.find(
  //         ({ parent_categories }) => parent[i]?._id === parent_categories
  //       )
  //     );
  //   }
  //   parentData?.push(
  //     parent?.filter(({ _id }) => {
  //       return list?.find(({ parent_categories }) => _id === parent_categories);
  //     })
  //   );
  //console.log(datalist);
};
